export class KanbanItemClass {
  constructor(id, title, members, attachments, labels, checklist) {
    this.id = id;
    this.title = title;
    this.members = members;
    this.attachments = attachments;
    this.labels = labels;
    this.checklist = checklist;
  }
}
