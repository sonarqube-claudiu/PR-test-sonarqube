type IssuesCounts {
     NEW: Int,
     IN_PROGRESS: Int,
     RESOLVED: Int,
     ON_HOLD: Int,
}

type Metadata {
     priority: defaultID,
     assigned_to: defaultID,
     author: defaultID,
     start_date: String,
     due_date: String,
     done_ratio: Int,
     closed_on: String,
     issues: IssuesCounts,
     stories: Int,
}